{
  "name": "ai-council-web",
  "private": true,
  "type": "module",
  "description": "AI Council web interface (static files served by the FastAPI backend; no build step).",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
